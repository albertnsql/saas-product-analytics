name: saas_analytics
version: '1.0.0'
config-version: 2

profile: saas_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  saas_analytics:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
      core:
        +materialized: table
      finance:
        +materialized: table
      product:
        +materialized: table